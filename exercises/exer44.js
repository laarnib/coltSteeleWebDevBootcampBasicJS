/* Define a function that returns the square of a number.
 * Define the function as a function expression, stored
 * in a variable called square. */

let square = function(number) {
    return number * number;
}

// For testing
square(4) //16
square(3) //9